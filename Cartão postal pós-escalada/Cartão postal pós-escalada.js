    var messaga;
    console.log(messaga);
        if (message.length > 80) {
            console.log('The message is too long for a post card');

        }
        
            if (message.length < 80) {
                console.log('The message fits on a postcard');
            }